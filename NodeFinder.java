public class NodeFinder {
    /**
     * Search the subtree rooted at this node and return the first Node object it finds whose .data field matches the
     * given target value.
     *
     * @param head the root of the subtree to search, null means an empty tree
     * @param target the target value to search for
     * @return the node within this subtree whose data field equals the target value, or null if no matching node is
     * found
     */
    public static Node find(Node head, int target) {
        // TODO: Implement me!
        return null;
    }
}
